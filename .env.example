# SheetDB API Configuration
# Get your SheetDB API URL from https://sheetdb.io
# The API URL is already set as default in the code, but you can override it here

SHEETDB_API_URL=https://sheetdb.io/api/v1/ax3gbf2cfplov

# Email Notification Configuration
# See EMAIL_SETUP.md for detailed setup instructions

# Enable or disable email notifications (set to 'true' to enable)
EMAIL_NOTIFICATIONS_ENABLED=true

# Your Gmail address (the account that will send the emails)
GMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# Generate this at: https://myaccount.google.com/apppasswords
GMAIL_APP_PASSWORD=your-16-character-app-password

# Email address to receive notifications (defaults to GMAIL_USER if not set)
NOTIFICATION_EMAIL=your-email@gmail.com
